# API CRUD Assignment

This is my node.js assignment for creating a product api.

## How to run
1. run `npm install` to download packages
2. run `npm run dev` to start the local server

## Features
- create new products
- get all products
- edit a product
- delete a product

The database I used is mongodb.
